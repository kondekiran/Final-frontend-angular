name: Deploy master
on:
  push:
    branches:
      - master
#ab
jobs:
  build:
    runs-on: windows-latest
    steps:
    - name: Checkout source code
      uses: actions/checkout@v3
      with:
        node-version: 12.x
    - name: Run ls command
      run: |
        dir 
